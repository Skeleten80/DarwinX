# DarwinX SDK

The SDK will eventually contain public headers/modules, libraries, developer tools, API versioning metadata and example projects.

No API is considered stable during the bootstrap milestones. Public/private boundaries and ABI compatibility policy will be defined before the first developer release.
